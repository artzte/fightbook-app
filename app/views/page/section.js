import Ember from "ember";

export default Ember.View.extend({
  //didInsertElement: function() {
    //console.log("SECTION die");
    //Ember.run.scheduleOnce('afterRender', this, function() {
      //Ember.$(window).trigger('resize');
    //});
  //}
});
