<div id="section-content">
  {{#each section in sections}}
  {{#link-to 'page.section' page.slug section.sortOrder class='section-link'}}{{/link-to}}
  <div class="translation">
    <p>{{section.translation.md}}</p>
  </div>
  {{/each}}
</div>
