{{#if model.footnotes}}
<div class="footnotes-wrapper">
  <ul class="no-bullet">
    {{#each model.footnotes as |footnote| }}
      <li><sup>{{footnote.reference}}</sup> {{{footnote.content}}}</li>
    {{/each}}
  </ul>
</div>
{{/if}}
